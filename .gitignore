# CLion / CMake build output
cmake-build-*/
build/

# Not part of the ledger reader: assistant files and generated notes
.claude/
docs/

# Regenerated by the app any time (exports and HTML reports).
# The rest of Saved_Data is the app's memory (merges, seat owners, adjustments,
# payments) and is kept, because the totals depend on it.
Saved_Data/reports/
Saved_Data/player_summary.csv
Saved_Data/settlements.csv
Saved_Data/style_stats.csv
Saved_Data/playstyle.csv

# Python cache from running tools/validate.py
__pycache__/

# macOS
.DS_Store
